<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Projekt TODO{% endblock %}</title>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.css" rel="stylesheet">

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('assets/CSS/bootstrap/bootstrap.css') }}">
    {% endblock %}
</head>

<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.js"></script>
<script>
    $(document).ready(function() {
        // you may need to change this code if you are not using Bootstrap Datepicker
        $('.js-datepicker').datepicker({
            format: 'yyyy-mm-dd H:i'
        });
    });
</script>

<body class="bg-secondary">

<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">ToDo Project</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('homepage') }}">Wszystkie zadania<span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('todo_task_create') }}">Dodaj nowe zadanie</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('invite') }}">Zaproś znajomego</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_logout') }}">Wyloguj się</a>
                    </li>
                {% else %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_register') }}">Zarejestruj się</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_login') }}">Zaloguj się</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>

<main>{% block body %}{% endblock %}</main>


<footer class="fixed-bottom text-muted bg-dark">
    <div class="container text-center my-2">
        <p>Copyrights &copy;: Bartłomiej Szyszkowski</p>
    </div>
</footer>


<script src="{{ asset('assets/js/jquery-3.5.1/jquery-3.5.1.min.js') }}"></script>
<script src="{{ asset('assets/js/bootstrap/bootstrap.bundle.js') }}"></script>
<script src="https://kit.fontawesome.com/b26e0d0c0b.js" crossorigin="anonymous"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
